import { analyzeMTF } from '../mtf/analyzer.mjs';
export function runBacktest({ fourHourCandles, oneHourCandles, fifteenMinuteCandles }) { const mtf = analyzeMTF(fourHourCandles, oneHourCandles, fifteenMinuteCandles); const signal = { symbol: 'BACKTEST', timeframe: '15m', action: mtf.action, confidence: mtf.confidence, reason: 'Backtest MTF analysis', mtf: { fourHour: mtf.fourHour, oneHour: mtf.oneHour, fifteenMinute: mtf.fifteenMinute }, structure: null, smcConfirmation: null, timestamp: Date.now() }; return { signals: [signal], trades: signal.action === 'WAIT' ? 0 : 1, wins: 0, losses: 0, winRate: 0, profitLoss: 0, maxDrawdown: 0 }; }
